{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary"><i class="bi bi-pen" style="color:white;font-size:20px;"></i>{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
